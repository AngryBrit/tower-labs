## TowerSmith {{VERSION}}

Release date: {{DATE}}

### Highlights

{{HIGHLIGHTS}}

### Fixes

{{FIXES}}

### Validation

{{VALIDATION}}

### Notes

- **TowerSmith** is the user-facing app name (browser title, PWA manifest, social previews). The GitHub repo is `tower-smith`; the npm package name `tower_export` is internal.
- `VERSION`, `package.json`, `package-lock.json`, and the release tag are expected to be aligned for each release.
- Full historical change detail lives in `CHANGELOG.md`.
